import React, { Component } from 'react';
import styled, { withTheme } from 'styled-components';

const styles = {};

export class Footer extends Component {
  render() {
    return <div>Hello world</div>;
  }
}

export default withTheme(styles)(Footer);
